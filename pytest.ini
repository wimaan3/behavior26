[pytest]
testpaths = tests
markers =
    openpi: needs the openpi fork checkout and a jax install (skipped otherwise)
# The openpi tests build and differentiate a real (tiny) Pi0 on CPU; a few
# seconds each is expected. Deselect them with: pytest -m "not openpi"
