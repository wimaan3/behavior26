# Analysis and harness tooling ONLY.
# The simulator lives in the separate `behavior` conda env created by BEHAVIOR-1K's setup script.
# Never pip-install these into that env.
pandas>=2.0
# merge_progress_labels.py reads/writes the LeRobot parquet directly, and
# tests/test_progress_labels.py importorskips on it -- without it that whole
# module silently fails to collect rather than reporting as skipped.
pyarrow>=14.0
pyyaml>=6.0
tabulate>=0.9

# GPU-free pipeline testing: policy/null_server.py + tests/mock_evaluator.py.
# Deliberately light -- the null server must run standalone in a bare venv.
websockets>=12.0
msgpack>=1.0
pytest>=7.0

# Optional, imported lazily where used:
#   scipy         analysis/compare.py -- exact t-interval; falls back to a normal
#                 approximation (and says so) when absent.
#   msgpack-numpy policy/wire.py -- required to send/receive real ndarray observations.
#                 The null policy and mock evaluator do not need it; the REAL evaluator
#                 almost certainly does. Install before the first GPU run.
# scipy>=1.10
# msgpack-numpy>=0.4.8
